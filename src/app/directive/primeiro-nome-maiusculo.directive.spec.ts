import { PrimeiroNomeMaiusculoDirective } from './primeiro-nome-maiusculo.directive';

describe('PrimeiroNomeMaiusculoDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimeiroNomeMaiusculoDirective();
    expect(directive).toBeTruthy();
  });
});
